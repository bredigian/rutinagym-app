import { SafeAreaView } from "react-native-safe-area-context"
import { Text } from "react-native"

export default function MusclesGroups() {
  return (
    <SafeAreaView>
      <Text>Muscle Groups Page</Text>
    </SafeAreaView>
  )
}
